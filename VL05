using System
	
namespace a
    {
 class Program
 {	
     static void Main(string[] args)
     {
         string s1 = Console.ReadLine();
        int n = Convert.ToInt32(s);
        int sum = 0;
        for (int i = 2; i<=n; i++)
            s = s + i;
        	s = s + 2 * n;
        Console.WriteLine(s);
    	}
 	}
}
